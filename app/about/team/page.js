import React from "react";

const TeamPage = () => {
  return (
    <div className="mx-12 my-10 py-5 flex flex-col ">
      <h1>Fitness Cloud</h1>
      <p className="mt-5">
        The Fitness cloud is built based on Exercise DB API. It is built with
        next 13, React , tailwind etc. Server side fetching with client side
        simple pagination with jotai npm. The new app directory is set to true
      </p>
    </div>
  );
};

export default TeamPage;
